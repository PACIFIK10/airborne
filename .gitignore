# Dependencies
node_modules/

# Secrets — never commit these. The whole point of .env is that it stays local
# and the real values live in Vercel's dashboard instead.
.env
.env.local
.env*.local

# Build output
dist/
.vercel/

# Editor / OS noise
.DS_Store
.vscode/
.qodo/
*.log
